type Message {
  id: ID! @id
  createdAt: DateTime! @createdAt
  content: String!
  length: String
  postedBy: User
  chat: String
}

type User {
  id: ID! @id
  name: String!
  email: String!
  password: String!
  messages: [Message!]!
}